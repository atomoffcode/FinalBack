var splide = new Splide( '.splide-product', {
    // type   : 'loop',
    drag   : 'free',
    perPage: 6,
    fixedWidth: '335px',
    pagination: false,
  } );
  splide.mount();

  var splideg = new Splide( '.glide-product', {
    // type   : 'loop',
  } );
  splideg.mount();

  
  var splide2 = new Splide( '.splide-product2', {
    // type   : 'loop',
    drag   : 'free',
    perPage: 6,
    fixedWidth: '335px',
    pagination: false,
  } );
  splide2.mount();

  var splideg2 = new Splide( '.glide-product2', {
    // type   : 'loop',
  } );
  splideg2.mount();

  var splide3 = new Splide( '.splide-product3', {
    // type   : 'loop',
    drag   : 'free',
    perPage: 6,
    fixedWidth: '335px',
    pagination: false,
  } );
  splide3.mount();