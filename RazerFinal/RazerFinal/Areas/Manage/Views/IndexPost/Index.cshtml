@model PageNatedList<IndexPost>
@{
    ViewData["Title"] = "Index";
    int count = (Model.PageIndex - 1) * 3;
}

<div class="row">
    <div class="col-lg-12">
        <div class="row justify-content-between">
            <div class="col-lg-3">
                <h1 class="h3 mb-4 text-gray-800">Landing Page Posts Page</h1>
            </div>
            <div class="col-lg-2">
                @if (User.Identity.IsAuthenticated && User.IsInRole("SuperAdmin"))
                {
                    <a asp-area="manage" asp-controller="indexpost" asp-action="create" class="btn btn-primary">Create</a>
                }
            </div>
        </div>
    </div>
    <div class="col-lg-12">
        <div class="indexContainer">
            @await Html.PartialAsync("_IndexPostIndexPartial",Model)

        </div>

    </div>

</div>


